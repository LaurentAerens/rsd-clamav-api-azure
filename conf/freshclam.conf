DatabaseMirror http://database.clamav.net
DNSDatabaseInfo current.cvd.clamav.net
CompressLocalDatabase yes
Foreground yes
# UpdateLogFile disabled for Docker - logs go to stdout
LogVerbose yes
MaxAttempts 2

# Optional additional signatures (community mirror).
# If this mirror is temporarily unavailable, startup still continues.
DatabaseCustomURL http://mirror.seichter.de/sanesecurity/rogue.hdb